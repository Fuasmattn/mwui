import { Meta, Typeset } from "@storybook/addon-docs";
import tokens from "@fuasmattn/mwui-token-farm/dist/json/global.json";

<Meta title="Typography" />

export const fontFamily = tokens.fontFamilies.default.value;

export const fontSizes = Object.values(tokens.fontSize).map((size) =>
  Number(size.value)
);

export const Text = ({ text }) => {
    return (
        <>{text}</>
    )
}

# Typography

**Font Family:** <Text text={fontFamily} />

<Typeset
  fontSizes={fontSizes}
  sampleText="Lorem ipsum dolor sit amet, consectetur adipiscing elit."
  fontFamily={tokens.fontFamilies.default.value}
/>
